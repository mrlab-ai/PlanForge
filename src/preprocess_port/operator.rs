#[derive(Debug, Clone)]
pub struct Operator {
    // TODO: port fields from numeric-fd/src/preprocess/operator.h
}

impl Operator {
    pub fn new() -> Self {
        todo!("port Operator::new from operator.cc");
    }

    pub fn dump(&self) {
        todo!("port Operator::dump from operator.cc");
    }
}
