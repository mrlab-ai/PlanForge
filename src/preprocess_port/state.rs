#[derive(Debug, Clone)]
pub struct State {
    // TODO: port fields from numeric-fd/src/preprocess/state.h
}

impl State {
    pub fn new() -> Self {
        todo!("port State::new from state.cc");
    }

    pub fn dump(&self) {
        todo!("port State::dump from state.cc");
    }
}
